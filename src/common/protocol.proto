syntax = "proto3";

package geeks;

message ChatMessage {
    enum MessageType {
        JOIN = 0;
        MESSAGE = 1;
        ERROR = 2;
        LEAVE = 3;
    }

    string from_user = 1;
    string text = 3;
    string room_id = 4;
    string password = 5;
    MessageType type = 6;
}